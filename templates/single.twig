{% extends "base.twig" %}

{% block content %}

<div class="row">
	<div class="col-sm-12">
		<h2 class="text-center">{{post.title}}</h2>
	</div>
</div>


<div class="row">
	<div class="col-sm-12">
		<div class="card">
			<div class="card-body">
				{{post.content}}
			</div>
		</div>
	</div>
</div>

{% endblock %}
